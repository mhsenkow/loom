# Core backend deps for Docker "just run it" image.
# Omit heavy optional deps (torch, playwright, cloud SDKs) for faster builds.
# For full image/music/web/cloud, use requirements.txt and a longer build.
fastapi>=0.109.1
python-multipart>=0.0.7
uvicorn[standard]>=0.27.0
python-socketio>=5.10.0
ollama>=0.1.6
chromadb>=0.4.22
pydantic>=2.5.0
python-dotenv>=1.0.0
httpx>=0.26.0
pymupdf>=1.23.0
psutil>=5.9.0
uvloop>=0.19.0
# Music (mock mode + wav write)
numpy>=1.24.0
soundfile>=0.12.1
scipy>=1.10.0
# Scheduler (cron jobs)
apscheduler>=3.10.0
sqlalchemy>=2.0.0
